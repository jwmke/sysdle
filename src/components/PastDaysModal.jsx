import Modal from './Modal'

export default function PastDaysModal({ isOpen, onClose }) {
  return (
    <Modal isOpen={isOpen} onClose={onClose} title="Past Days">
      {/* Empty for now */}
    </Modal>
  )
}
